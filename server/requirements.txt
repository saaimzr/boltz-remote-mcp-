# Boltz MCP Server Dependencies

# FastMCP 2.0 - The fast, Pythonic way to build MCP servers
# Provides decorators and utilities for building MCP servers with HTTP transport
fastmcp>=2.0.0

# Boltz - Protein structure prediction model
# Note: Install separately with: pip install boltz[cuda] -U
# This will install PyTorch, transformers, and other ML dependencies
# Requires: Python 3.10-3.12, CUDA-capable GPU

# Additional utilities
python-dotenv>=1.0.0  # For loading environment variables from .env files
